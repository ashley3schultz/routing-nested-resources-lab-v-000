<ul>
<% @songs.each do |song| %>
  <li><%= link_to display_artist(song), artist_song_path(song.artist, song) %> - <%= link_to song.title, song_path(song) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
